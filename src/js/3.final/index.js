export default function final() {

  return new Promise(function(resolve) {

    // when it is all done mb

    // 1.Disable twitter balls
    // dispose all stuff if needed


    // 2. Run uniform animation to reveal plane texture

    // 3. Push play, and remove triangles edges.




    // resolve();
  });
}
